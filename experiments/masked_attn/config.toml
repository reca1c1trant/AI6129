output_dir = '/scratch/users/ntu/song0304/loraedit_exp/experiments/masked_attn/lora'
dataset = '/home/users/ntu/song0304/code/experiments/masked_attn/dataset.toml'

epochs = 100
micro_batch_size_per_gpu = 1
pipeline_stages = 1
gradient_accumulation_steps = 1
gradient_clipping = 1
warmup_steps = 0

eval_every_n_epochs = 1000000
eval_before_first_step = true
eval_micro_batch_size_per_gpu = 1
eval_gradient_accumulation_steps = 1

save_every_n_epochs = 50
checkpoint_every_n_minutes = 1000000000
activation_checkpointing = 'unsloth'
partition_method = 'parameters'
save_dtype = 'bfloat16'
caching_batch_size = 1
steps_per_print = 1
video_clip_mode = 'single_beginning'
blocks_to_swap = 24

[model]
type = 'wan'
ckpt_path = '/scratch/users/ntu/song0304/checkpoints/Wan2.1-I2V-14B-480P'
dtype = 'bfloat16'
transformer_dtype = 'bfloat16'
timestep_sample_method = 'uniform'

[adapter]
type = 'lora'
rank = 16
dtype = 'bfloat16'
exclude_linear_modules = ["k_img", "v_img"]

[lr_scheduler]
type = 'cosine'
eta_min = 1e-6

[optimizer]
type = 'AdamW8bitKahan'
lr = 0.0005
betas = [0.9, 0.99]
weight_decay = 0.01
stabilize = true
